<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 本来需要配置数据源，但ssm整合后交给Spring去做了 -->

    <!-- 别名 -->
    <typeAliases>
        <package name="com.sergio.pojo"/>
    </typeAliases>
    <mappers>
        <mapper resource="com/sergio/dao/BookMapper.xml"/>
    </mappers>
</configuration>